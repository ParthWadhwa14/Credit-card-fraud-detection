name: Build and Publish Docker Images

on:
  push:
    branches:
      - main

  workflow_dispatch:

jobs:
  docker-publish:
    name: Build and Push FastAPI and Streamlit Images
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push FastAPI image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/fraud-detection-api:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/fraud-detection-api:${{ github.sha }}

      - name: Build and push Streamlit image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile.streamlit
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/fraud-streamlit-ui:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/fraud-streamlit-ui:${{ github.sha }}