apiVersion: v1
kind: Service
metadata:
  name: fraud-streamlit-service
  namespace: fraud-detection
  labels:
    app: fraud-streamlit-ui
spec:
  selector:
    app: fraud-streamlit-ui
  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
  type: ClusterIP